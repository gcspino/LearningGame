<Window x:Class="LearningGame.GUI.BattleTriangleFact"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:LearningGame.GUI"
        xmlns:local="clr-namespace:LearningGame.GUI"
        mc:Ignorable="d"
        Title="Learning Game - Battle" Height="800" Width="1155">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="25" />
            <ColumnDefinition Width="300" />
            <ColumnDefinition Width="490" />
            <ColumnDefinition Width="300" />
            <ColumnDefinition Width="25" />
        </Grid.ColumnDefinitions>
        <controls:CombatantView x:Name="LeftCombatant" Grid.Row="1" Grid.Column="1" DataContext="{Binding LeftCombatantViewModel}"/>
        <controls:CombatantView x:Name="RightCombatant" Grid.Row="1" Grid.Column="3" DataContext="{Binding RightCombatantViewModel}"/>
        <controls:TriangleFactView x:Name="Question" Grid.Row="1" Grid.Column="2" DataContext="{Binding QuestionViewModel}"/>
        <TextBox Grid.Row="3" Grid.Column="2" Name="Answer"  FontSize="72" HorizontalAlignment="Center" MinWidth="100" Keyboard.KeyDown="Answer_KeyDown"/>
        <MediaElement Name="BackgroudMedia" Source="Images\\Battle_02.wav" MediaEnded="MediaElement_MediaEnded" Volume="{Binding MusicVolume}" />
        <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding PlayerGoldText, FallbackValue=GOLDTEXT}" FontSize="22" HorizontalAlignment="Left" Margin="7"/>
        
        <TextBlock Grid.Row="4" Grid.Column="1" Text="Challenge" FontSize="22" HorizontalAlignment="Right" Margin="7"/>
        <Slider Grid.Row="4" Grid.Column="2" Minimum="6" TickFrequency="1" TickPlacement="Both"  Maximum="10" Height="30" Width="350" HorizontalAlignment="Left"  Value="{Binding Challenge, FallbackValue=5}" Margin="7" />
        <TextBlock Grid.Row="4" Grid.Column="2" HorizontalAlignment="Right" Text="{Binding Challenge, FallbackValue=10}" Width="30" FontSize ="22" Margin="7" Height="30"/>
        <CheckBox Grid.Row ="4" Grid.Column="3" Content="Boss Mode" IsChecked="{Binding BossMode}" VerticalAlignment="Center" FontSize="22" Margin="7"/>
        <CheckBox Grid.Row ="5" Grid.Column="3" Content="Voice Mode" IsChecked="{Binding VoiceMode}" VerticalAlignment="Center" FontSize="22" Margin="7"/>
        <ComboBox Grid.Row ="6" Grid.Column="3" ItemsSource="{Binding Operators}" SelectedItem="{Binding Operator, FallbackValue=x}" VerticalAlignment="Center" FontSize="22" Margin="7"/>
        <TextBlock Grid.Row="5" Grid.Column="1" Text="Factor" FontSize="22" HorizontalAlignment="Right" Margin="7"/>
        <TextBox Grid.Row="5" Grid.Column="2" Height="50" FontSize ="22" Text="{Binding MultFactor}" Margin="7" />

        <TextBlock Grid.Row="6" Grid.Column="1" Text="{Binding SessionTimeStatus, FallbackValue=SESSIONTIMESTATUS}" FontSize="22" HorizontalAlignment="Right" Margin="7"/>
        <Button Grid.Row="6" Grid.Column="2" Height="50" Content="Battle!!" Click="Button_Click"/>
        <TextBox Grid.Row="7" Grid.Column="1" Height="50" Width ="150" HorizontalAlignment="Left" FontSize ="22" Text="{Binding GoldPayment}" Margin="7" />
        <Button Name="Payment" Grid.Row="7" Grid.Column="1" Height="50" Width="120" HorizontalAlignment="Right" Content="Pay w/ Secret Code" Click="Payment_Click"/>
        <TextBlock Grid.Row="7" Grid.Column="2" Height="50" Text="{Binding GameStatusText, FallbackValue=Battle...}" FontSize="22" HorizontalAlignment="Center" Margin="7" />
    </Grid>
</Window>
