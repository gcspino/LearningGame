<UserControl x:Class="LearningGame.GUI.TriangleFactView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LearningGame.GUI"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="195" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="195" />
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding ProblemTop, FallbackValue=??}" HorizontalAlignment="Center"  FontSize="72"/>
        <TextBlock Grid.Row="2" Grid.Column="0" Grid.ColumnSpan ="3" Text="{Binding MiddleValue, FallbackValue=* /}" HorizontalAlignment="Center"  FontSize="72"/>
        <TextBlock Grid.Row="2" Grid.Column="0" Text="{Binding ProblemLeft, FallbackValue=??}" HorizontalAlignment="Center"  FontSize="72"/>
        <TextBlock Grid.Row="2" Grid.Column="2" Text="{Binding ProblemRight, FallbackValue=??}" HorizontalAlignment="Center"  FontSize="72"/>
    </Grid>
</UserControl>
